include ':app'

// Note: Capacitor plugins are automatically added by capacitor.settings.gradle
// which is applied by the Capacitor CLI during sync.
// DO NOT manually apply build scripts here as it causes scope errors.
